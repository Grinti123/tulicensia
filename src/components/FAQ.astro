<section class="py-16 bg-white">
  <div class="container mx-auto px-4 md:px-8 max-w-7xl">
    <!-- Section Title -->
    <div class="text-center mb-10">
      <h2 class="text-3xl font-bold text-green-700">Preguntas frecuentes</h2>
    </div>

    <!-- Tab Navigation -->
    <div class="flex flex-wrap justify-center gap-4 mb-12">
      <button class="py-3 px-8 rounded-full bg-green-700 text-white font-medium focus:outline-none" id="tab-general">
        General
      </button>
      <button class="py-3 px-8 rounded-full border border-green-700 text-green-700 font-medium hover:bg-green-50 focus:outline-none" id="tab-proceso">
        Proceso
      </button>
      <button class="py-3 px-8 rounded-full border border-green-700 text-green-700 font-medium hover:bg-green-50 focus:outline-none" id="tab-tiempo">
        Tiempo
      </button>
      <button class="py-3 px-8 rounded-full border border-green-700 text-green-700 font-medium hover:bg-green-50 focus:outline-none" id="tab-costos">
        Costos
      </button>
    </div>

    <!-- FAQ Accordion -->
    <div class="mx-auto w-full max-w-[100%]">
      <div class="space-y-4">
        <!-- FAQ Item 1 -->
        <div class="border-b border-gray-200 pb-4">
          <button class="flex justify-between items-center w-full mX-W-[100%] text-left focus:outline-none group" aria-expanded="false" aria-controls="faq-1">
            <h3 class="text-lg font-medium text-green-700">¿En cuánto tiempo recibo mi licencia?</h3>
            <svg class="w-6 h-6 text-green-700 transform group-aria-expanded:rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div id="faq-1" class="mt-2 hidden">
            <p class="text-gray-600">
              El tiempo de entrega varía según el tipo de trámite. Para renovaciones simples, el proceso puede tomar entre 7 a 10 días hábiles. Para trámites más complejos que requieren verificación adicional, puede tomar hasta 15 días hábiles.
            </p>
          </div>
        </div>

        <!-- FAQ Item 2 -->
        <div class="border-b border-gray-200 pb-4">
          <button class="flex justify-between items-center w-full text-left focus:outline-none group" aria-expanded="false" aria-controls="faq-2">
            <h3 class="text-lg font-medium text-green-700">¿Puedo recibir el examen medico "online"?</h3>
            <svg class="w-6 h-6 text-green-700 transform group-aria-expanded:rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div id="faq-2" class="mt-2 hidden">
            <p class="text-gray-600">
              Sí, ofrecemos exámenes médicos a través de consultas por video con médicos certificados. Este servicio está disponible para ciertos trámites de renovación. Sin embargo, para licencias iniciales o algunos casos especiales, puede ser necesario un examen médico presencial.
            </p>
          </div>
        </div>

        <!-- FAQ Item 3 -->
        <div class="border-b border-gray-200 pb-4">
          <button class="flex justify-between items-center w-full text-left focus:outline-none group" aria-expanded="false" aria-controls="faq-3">
            <h3 class="text-lg font-medium text-green-700">¿Cuáles son los costos de los trámites?</h3>
            <svg class="w-6 h-6 text-green-700 transform group-aria-expanded:rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div id="faq-3" class="mt-2 hidden">
            <p class="text-gray-600">
              Los costos varían según el tipo de trámite. Nuestras tarifas incluyen todos los sellos y aranceles gubernamentales, más nuestra comisión por gestión. En tu cuenta podrás ver un desglose detallado de todos los costos asociados a tu trámite específico.
            </p>
          </div>
        </div>

        <!-- FAQ Item 4 -->
        <div class="border-b border-gray-200 pb-4">
          <button class="flex justify-between items-center w-full text-left focus:outline-none group" aria-expanded="false" aria-controls="faq-4">
            <h3 class="text-lg font-medium text-green-700">¿Puedo recibir ayuda de un representante?</h3>
            <svg class="w-6 h-6 text-green-700 transform group-aria-expanded:rotate-180 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>
          <div id="faq-4" class="mt-2 hidden">
            <p class="text-gray-600">
              Absolutamente. Contamos con representantes disponibles por teléfono, chat y WhatsApp para asistirte en cada paso del proceso. Puedes comunicarte con nuestro equipo en horario laboral o programar una cita virtual para recibir atención personalizada.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Accordion functionality
  document.addEventListener('DOMContentLoaded', () => {
    const accordionButtons = document.querySelectorAll('button[aria-controls^="faq-"]');

    accordionButtons.forEach(button => {
      button.addEventListener('click', () => {
        const isExpanded = button.getAttribute('aria-expanded') === 'true';
        const contentId = button.getAttribute('aria-controls');
        if (!contentId) return;

        const content = document.getElementById(contentId);
        if (!content) return;

        // Toggle current accordion
        button.setAttribute('aria-expanded', isExpanded ? 'false' : 'true');

        // Toggle visibility
        if (isExpanded) {
          content.classList.add('hidden');
        } else {
          content.classList.remove('hidden');
        }
      });
    });

    // Tab functionality
    const tabButtons = [
      document.getElementById('tab-general'),
      document.getElementById('tab-proceso'),
      document.getElementById('tab-tiempo'),
      document.getElementById('tab-costos')
    ].filter((tab): tab is HTMLElement => tab !== null);

    tabButtons.forEach(tab => {
      tab.addEventListener('click', () => {
        // Reset all tabs
        tabButtons.forEach(t => {
          t.classList.remove('bg-green-700', 'text-white');
          t.classList.add('border', 'border-green-700', 'text-green-700');
        });

        // Highlight active tab
        tab.classList.remove('border', 'border-green-700', 'text-green-700');
        tab.classList.add('bg-green-700', 'text-white');

        // Here you would add logic to show content relevant to the selected tab
        // For example, filtering different sets of FAQs
      });
    });
  });
</script>
